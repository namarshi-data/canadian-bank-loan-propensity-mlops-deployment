apiVersion: v1
kind: Service
metadata:
  name: bank-loan-propensity-service
spec:
  type: LoadBalancer
  selector:
    app: bank-loan-propensity-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
